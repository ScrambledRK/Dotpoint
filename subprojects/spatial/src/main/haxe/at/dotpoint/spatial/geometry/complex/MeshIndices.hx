package at.dotpoint.spatial.geometry.complex;

/**
 * 
 * @author RK
 */
typedef MeshIndexTriangle   = Int;  // index of a face (composed of 3 vertices)
typedef MeshIndexVertex     = Int;  // index of a vertex (composed of multiple registers)
typedef MeshIndexData   	= Int;  // index of a single vertex data (e.g. position)